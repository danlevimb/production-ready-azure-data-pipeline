# Sample Data

This folder will contain small retail/order-processing CSV files for the MVP.

Candidate files:

```text
orders_YYYYMMDD.csv
payments_YYYYMMDD.csv
order_items_YYYYMMDD.csv
```

The data should remain small because the project focuses on production readiness, not volume.
