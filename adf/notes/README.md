# ADF JSON Reference Artifacts

The JSON files in this folder are reference artifacts.

They document the intended ADF dataset and pipeline definitions for the MVP.

Depending on the ADF authoring mode and Git integration settings, these files may need adjustment before direct import.

For this phase, the recommended path is to build the dataset and pipeline in ADF Studio using the execution guide, then publish from ADF Studio.
